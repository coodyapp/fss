{
  "name": "@fss/www",
  "version": "0.1.0",
  "private": true,
  "description": "FSS landing page — static site deployed to Cloudflare Pages",
  "scripts": {
    "deploy": "npx wrangler pages deploy public --project-name coody-fss-www-prd-01"
  }
}
