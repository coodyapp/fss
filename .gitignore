# macOS
.DS_Store

# Dependencies (there should be none, but never commit them)
node_modules/

# Local reference material and session state — not part of the product
tmp/
.remember/

# Wrangler local state
.wrangler/
